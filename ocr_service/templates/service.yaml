apiVersion: v1
kind: Service
metadata:
  name: {{ include "ocr_service.fullname" . }}
  labels:
    {{- include "ocr_service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ocr_service.selectorLabels" . | nindent 4 }}
